fastapi
uvicorn
python-multipart
sqlalchemy
python-jose[cryptography]
passlib[bcrypt]
python-dotenv
pydantic
pydantic-settings
email-validator
# Only needed if DATABASE_URL in .env points at Postgres instead of SQLite —
# safe to leave installed either way.
psycopg2-binary